#
# Copyright (c) 2025 Nordic Semiconductor ASA
#
# SPDX-License-Identifier: LicenseRef-Nordic-5-Clause
#

SB_CONFIG_BOOTLOADER_MCUBOOT=y
SB_CONFIG_PARTITION_MANAGER=n

# nRF5340 (nrf7002dk) only: the BLE controller runs on the network core
# over IPC. Build the upstream Zephyr hci_ipc sample as the netcore image
# so the Wi-Fi provisioning over BLE module can open the HCI driver.
# On nrf54lm20dk SUPPORT_NETCORE=n (single-core SoC), so this setting
# has no effect there and produces a benign Kconfig choice warning.
# kconfiglib prohibits 'select' on choice symbols, so this assignment cannot
# be made board-conditional from Kconfig.sysbuild.  The warning is harmless:
# sysbuild skips the hci_ipc image whenever SUPPORT_NETCORE=n, regardless of
# this choice assignment.  See also the MBEDTLS_PSA_CRYPTO_EXTERNAL_RNG note
# in boards/nrf7002dk_nrf5340_cpuapp.conf — same class of unfixable warning.
SB_CONFIG_NETCORE_HCI_IPC=y
